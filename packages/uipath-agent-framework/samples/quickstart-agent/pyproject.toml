[project]
name = "quickstart-agent"
version = "0.0.1"
description = "Quickstart Agent Framework agent example"
authors = [{ name = "John Doe" }]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "uipath",
    "uipath-agent-framework",
    "agent-framework-core>=1.0.0b260212",
    "httpx>=0.27.0",
]

[dependency-groups]
dev = [
    "uipath-dev",
]

[tool.uv]
prerelease = "allow"
