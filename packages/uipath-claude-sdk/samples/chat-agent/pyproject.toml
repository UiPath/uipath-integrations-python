[project]
name = "claude-chat-agent"
version = "0.0.1"
description = "UiPath Claude Agent SDK Conversational Agent"
authors = [{ name = "John Doe", email = "john.doe@myemail.com" }]
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.11"
dependencies = [
    "uipath-claude-sdk",
    "uipath-dev>=0.0.84",
]

[dependency-groups]
dev = [
    "uipath-dev",
]

[tool.uv.sources]
uipath-claude-sdk = { path = "../../", editable = true }
