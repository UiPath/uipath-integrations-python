[project]
name = "graph-flow"
version = "0.0.1"
description = "PydanticAI graph-based control flow example with pydantic-graph"
authors = [{ name = "John Doe" }]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "uipath",
    "uipath-pydantic-ai",
    "pydantic-ai>=0.1.0",
]

[dependency-groups]
dev = [
    "uipath-dev",
]
