[project]
name = "template-agent"
version = "0.0.1"
description = "template-agent"
authors = [{ name = "John Doe", email = "john.doe@myemail.com" }]
dependencies = [
    "uipath-llamaindex[bedrock,vertex]",
]
requires-python = ">=3.11"

[tool.uv.sources]
uipath-llamaindex = { path = "../../", editable = true }
